---
const { text, link, target } = Astro.props

import svgHl from '../assets/hl.svg'
---

<div class="relative group">
   <a href={link} target={target} class="bg-[var(--blue)] text-md font-medium flex items-center h-12 px-6 border-3 themed-border rounded-full" style="color: var(--text-color);">{text}</a>
   <img src={svgHl.src} width="30"  class="absolute -right-7 -bottom-3 transition-all duration-200 group-hover:-bottom-4 themed-svg" />
</div>